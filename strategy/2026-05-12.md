# Strategy Heartbeat — 2026-05-12

## State snapshot

| Metric | Value | Notes |
|---|---|---|
| open issues | 3 | #1 (blocked), #3 (blocked), #7 (built/verifier-dispatched) |
| done last 7d | 1 | #2 (IPC bridge, merged) |
| active_issue_count | 3 | vs concurrency_target=18 (17%) |
| dogfood_substitution_7d | 0 | no UI surface merged yet |
| surface_parity_pct | ~5% | inbox PR blocked; thread detail unmerged |
| design_score_v1 | n/a | no UI surface passed design gate yet (issue 3 still FAIL) |
| human-gate count | 2 | #1 and #3 need operator rebase |

## Issue audit (heartbeat 2 — ~07:55 UTC)

### Issue 1 — Boot scaffold (BLOCKED)
- Verifier PASS (3x) but `gh pr merge` fails: merge conflicts with PR #6 (IPC bridge restructured src/ to app/src/).
- Also hit GitHub API rate limit on earlier merge attempts.
- **Human action required**: operator must rebase `issue-1-scaffold` branch on main and re-push. PR #5.
- tokens.ts not yet on main — ALL future UI issues depend on this unblocking.

### Issue 2 — IPC bridge (DONE, merged)
- PR #6 merged. Base URL, account/thread/message CRUD live on main.

### Issue 3 — Inbox view (BLOCKED)
- Verifier PASS on current PR but same conflict with PR #6 scaffold rewrite.
- **Human action required**: operator must rebase `issue-3-inbox` branch after #1 lands.
- Prior design failures (VH=4, R=2, P=5) were on older builds. Latest verifier round issued PASS (VH=8, R=9, P=8).
- depends_on: #1 (tokens.ts must exist on main before #3's PR makes sense)

### Issue 7 — Thread detail / message reading (BUILT — verifier dispatched)
- Builder PR #8 landed: 14 commits, design self-score 8.5/9/9, 81/81 tests pass.
- Verifier dispatched but no verdict event yet in this heartbeat window.
- This is the first surface with a credible design score — critical to watch.

## Human-gate flag

**TWO issues (#1 and #3) are at human-gate.** Both need operator rebase on main.
Resolution order: #1 first (unblocks tokens.ts), then #3.
This is the primary bottleneck for `dogfood_substitution_7d` — no surfaces can ship until scaffold tokens land on main.

## Pattern observations (updated)

1. **Rebase debt accumulates fast**: PR #6 (IPC bridge) restructured src/ to app/src/ and created conflicts for every branch opened before it. Future issues should always branch from latest main, not from a stale branch.
2. **GitHub rate limits**: verifier hit API rate limits on merge attempts (~07:37-07:53 window). This is a shared token; high concurrency of verifiers all trying to merge creates contention. Worth noting as a scaling concern if active_issue_count grows.
3. **Same-failure loop resolved**: issue 3's 3rd verifier pass came through with VH=8, R=9, P=8 — the builder eventually fixed the R refresh bug and contrast issue. However the PR is now blocked by conflict, not by design. Root cause fixed, lesson: the builder template fix (reproduce-attack-before-resubmit language) is still worth adding once #1 lands and we have more surfaces shipping.
4. **Token-contrast fix still not on main**: #9ca3af → #6b7280 for text-tertiary remains a pending change (issue #1 branch). Until #1 merges, all downstream UI issues may inherit the broken token. Builder for #7 should have overridden locally on their branch.
5. **Compose is the critical path gap**: inbox + thread detail give read-only email. Without compose, dogfood_substitution_7d cannot reach 7 days — user must open another app to send. Compose is the highest-priority next surface.

## Actions this heartbeat

- [x] Updated sb-git strategy notes
- [x] Opened issue #11 (human-gate): operator rebase of #1 and #3 on main — tagged `human-gate`, reasoning: both PRs PASS verifier but blocked by conflict from PR #6 restructure
- [x] Opened issue #9: Compose window (new email + reply) — reasoning: compose is critical path for dogfood_substitution_7d; missing surface; depends_on #1/#3/#7
- [x] Opened issue #10: Search surface — reasoning: 2nd-most-used mobile surface; dogfood usability cliff at >50 threads; depends_on #1/#3/#7
- [ ] No stale issues to close (all 3 open issues actively in flight)
- [ ] No feedback-channel data to cluster (no reports received)

## Hypotheses retired this heartbeat

- ~~"Issue 3 builder won't fix same failure 3x"~~ — Issue 3 builder did fix it on 3rd attempt (VH=8/R=9/P=8 pass). Failure was in first two dispatches only.

## Next surfaces to queue (BFS from mobile-Genmail parity)

Priority order (each needs #1 to land first for tokens.ts):
1. **Compose / reply modal** (#9 opened this heartbeat) — blocks dogfood_substitution_7d
2. **Search surface** (#10 opened this heartbeat) — 2nd-most-used mobile surface
3. **Settings / account switcher** — needs after compose to handle multi-account flows
4. **Notification center / unread badge** — dock badge + OS notification triggers

## design_score_v1 watch

Issue 7 (thread detail) is the first surface with builder self-score ≥ 8.5 on all axes. If verifier confirms, rolling median will be initialized at VH=8.5, R=9, P=9 — excellent baseline. Watch for verifier verdict this tick.
